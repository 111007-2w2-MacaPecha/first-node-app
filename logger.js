let url = 'http://myurl.com';

log = (message1, message2) => {
  console.log(message1, message2);
};

// module.exports.url = url; //aca se exporta de a uno
module.exports.log = log; //aca se exporta de a uno